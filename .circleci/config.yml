 version: 2.1
jobs:
  hello-world:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Say Hello
          command: echo "Hello, World!"
workflows:
  build-and-test:
    jobs:
      - hello-world
